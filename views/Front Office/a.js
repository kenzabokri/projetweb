/*<script>
    // Récupérer la date de début de l'événement depuis PHP
    var dateDebutEvenement = "<?php echo $event['datedebut']; ?>";

    // Convertir la date de début en objet Date JavaScript
    var dateDebutEvenementObj = new Date(dateDebutEvenement);

    // Définir la date actuelle
    var dateActuelle = new Date();

    // Calculer la différence en jours entre la date de début et maintenant
    var differenceJours = Math.floor((dateDebutEvenementObj - dateActuelle) / (1000 * 60 * 60 * 24));

    // Afficher un message de rappel s'il reste moins de 7 jours pour l'événement
    if (differenceJours > 0 && differenceJours <= 7) {
        alert("Il reste moins d'une semaine pour arriver à l'événement !");
    }
</script>*/
